spring:
  application:
    name: customer
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
